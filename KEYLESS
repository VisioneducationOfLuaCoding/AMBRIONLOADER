-- Load Orion Library
local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/darkkcontrol/Roblox-Orion-UI-Libary-OP-UI-LIBARY-/refs/heads/main/source"))()

-- Create Window
local Window = OrionLib:MakeWindow({
    Name = "Ambrion Loader | ALL KEYLESS!",
    HidePremium = false,
    SaveConfig = false,
    IntroText = "Loading Ambrion..."
})

-- Create Tab
local Tab = Window:MakeTab({
    Name = "Loaders",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Arsenal Loader Button
Tab:AddButton({
    Name = "Arsenal",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/VisioneducationOfLuaCoding/Arsenal/refs/heads/main/Ambrion"))()
    end
})

-- Blade Ball Loader Button
Tab:AddButton({
    Name = "Blade Ball",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/VisioneducationOfLuaCoding/BladeBall/refs/heads/main/ScriptLoader", true))()
    end
})

-- Grow a Garden Loader Button
Tab:AddButton({
    Name = "Grow a Garden",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/VisioneducationOfLuaCoding/growagarden/refs/heads/main/Ambrion", true))()
    end
})

-- Steal a Brainrot Loader Button
Tab:AddButton({
    Name = "Steal a Brainrot",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/VisioneducationOfLuaCoding/StealABrainrot/refs/heads/main/Ambrion"))()
    end
})

-- Rivals V3 Loader Button
Tab:AddButton({
    Name = "Rivals V3",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/VisioneducationOfLuaCoding/Ambrion/refs/heads/main/RivalsVersion3", true))()
    end
})

-- bball legends
Tab:AddButton({
    Name = "BasketBall Legends",
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/VisioneducationOfLuaCoding/basketball-legends/refs/heads/main/ambrion%20v1'))()
    end
})

-- Ink Game
Tab:AddButton({
    Name = "Ink Game",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/VisioneducationOfLuaCoding/Ink-Game/refs/heads/main/Ambrion"))()
    end
})

-- Cut trees
Tab:AddButton({
    Name = "Cut Trees",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/VisioneducationOfLuaCoding/CutTrees/refs/heads/main/AmbrionLogs"))()
    end
})

-- Init
OrionLib:Init()

--// Ambrion Status Bar UI (Top Left, Shorter Width)
-- LocalScript inside StarterPlayerScripts

local RunService = game:GetService("RunService")
local Stats = game:GetService("Stats")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "StatusBar"
screenGui.ResetOnSpawn = false
screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- Main container (shorter width)
local container = Instance.new("Frame")
container.Name = "StatusContainer"
container.Size = UDim2.new(0, 550, 0, 24) -- reduced width
container.Position = UDim2.new(0, 5, 0, 5)
container.BackgroundColor3 = Color3.fromRGB(30, 30, 30) -- dark gray
container.BorderSizePixel = 0
container.Parent = screenGui

-- Red line at the top (matches container width)
local redLine = Instance.new("Frame")
redLine.Name = "TopLine"
redLine.Size = UDim2.new(1, 0, 0, 2) -- spans full container width
redLine.Position = UDim2.new(0, 0, 0, 0)
redLine.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
redLine.BorderSizePixel = 0
redLine.Parent = container

-- TextLabel
local statusLabel = Instance.new("TextLabel")
statusLabel.Name = "StatusText"
statusLabel.Size = UDim2.new(1, -10, 1, 0)
statusLabel.Position = UDim2.new(0, 5, 0, 0)
statusLabel.BackgroundTransparency = 1
statusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
statusLabel.Font = Enum.Font.Code
statusLabel.TextSize = 14
statusLabel.TextXAlignment = Enum.TextXAlignment.Left
statusLabel.Text = ""
statusLabel.Parent = container

-- Config
local scriptName = "Ambrion"
local version = "Status"

-- Update loop
RunService.RenderStepped:Connect(function()
    local fps = math.floor(1 / RunService.RenderStepped:Wait())
    local ping = Stats.Network.ServerStatsItem["Data Ping"]:GetValue()
    local time = os.date("%H:%M:%S")

    statusLabel.Text = string.format("%s | %s | FPS: %d | Ping: %d | %s", scriptName, version, fps, ping, time)
end)

